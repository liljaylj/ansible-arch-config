# vim: ft=yaml.ansible
---

- hosts: local
  roles:
    - archconfig_user
    - user
    - timedate
    - locale
    - hostname
    - ssd
    - sudoers
    - pacman
    - paru
    - kernels
    - needrestart
    - mkinitcpio
    - bootloader
    - zram
    - systemd
    - zsh
    - networkmanager
    - vconsole
    - solokeys
    - firewalld
    - nextdns
    # TODO: wireguard
    - pkgfile
    - realtime
    - profile
    - ssh
    # - ly
    # - kmscon
    - tmux
    - rust
    - docker
    - kube-tools
    - apps_cli
    # hardware
    - audio
    - bluetooth
    - intel_graphics
    # gui
    - fonts
    # - theme
    # - sway
    - plasma-desktop
    - apps_gui
    - tor
    - firefox
    - steam
    # WARN: neet to refactor
    - printer
    - avahi
    - samba
    - virtualization
    - snapper  # should be last (do not create snapshots when ansible install packages)

# TODO: use tags to split vsconsole only and gui only tasks. Eg. tor(cli) and torbrowser-launcher(gui)
