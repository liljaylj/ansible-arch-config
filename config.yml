# vim: ft=yaml.ansible
---

- name: Config
  hosts: all
  roles:
    - archconfig_user
    - user
    - timedate
    - locale
    - hostname
    - ssd
    - sudoers
    - pacman
    - fwupd
    - devtools
    - kernels
    - needrestart
    - mkinitcpio
    - bootloader
    - zram
    - zsh
    - networkmanager
    - firewalld
    - avahi
    - samba
    - vconsole
    - nextdns
    # TODO: wireguard
    - realtime
    - profile
    - power_management
    - smartcard
    - ssh
    # - ly
    # - kmscon
    - tmux
    - docker
    - kube_tools
    - pixelserv_tls
    - apps_cli

    # hardware
    - audio
    - bluetooth
    # - intel_graphics
    - amd_graphics

    # gui
    - fonts
    # - theme
    # - sway
    - plasma_desktop

    - apps_gui

    - flatpak
    - tor
    - firefox
    - games
    - printer
    - virtualization
    - snapper  # should be last (do not create snapshots when ansible install packages)

# TODO: use tags to split vsconsole only and gui only tasks. Eg. tor(cli) and torbrowser-launcher(gui)
# TODO: podman, buildah, skopeo, ... instead of docker
# TODO: install wireshark and add user to group wireshark
